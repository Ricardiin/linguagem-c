//Primeiro programa em linguagem c
#include <stdio.h>
//(main) programa principal
int main (){
		printf ("Hello, World!\n"); //saida do gello word para tela do computador, (\n) para quebrar a linha return 0; // sempre a ultima coisa do código
}
